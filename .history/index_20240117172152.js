const counterScreen = document.getElementById("countDisplay");
const Buttons = document

const counter = () => {
    let count = 0;
    if (increaseButton) {
        return count += 1
        console.log(count)  
    }
}

